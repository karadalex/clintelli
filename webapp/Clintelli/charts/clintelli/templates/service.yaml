apiVersion: v1
kind: Service
metadata:
  name: {{ template "clintelli.fullname" . }}
  labels:
    app: {{ template "clintelli.name" . }}
    chart: {{ template "clintelli.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ template "clintelli.name" . }}
    release: {{ .Release.Name }}
